TODO:
1. 有个零状态（伸缩杆最低，旋转手臂收起），通电归零，Disable状态归零。
2. 归零功能顺序必须是:先放下伸缩杆，再收起旋转手臂
3. 旋转手臂Enable后立即从0状态调整到base状态，base状态不会影响伸缩杆上下移动。整个Enable状态都不允许回到0状态。

状态罗列：
1. 零状态
2. base状态，等待珊瑚滑落，Enable默认
3. 4个插珊瑚状态，高度不同，旋转手臂角度不同
4. 1个抓球状态
5. 1个持球状态
另外需要释放按钮，是否球或者珊瑚，释放后立即回到base状态

PID调发：
1. 先逐步增加P，让稳态误差足够小
2. 增加I，让后段速度增加，消除稳态误差
3. 一旦出现震荡，增加一点D，注意D太大会增加震荡调整的时间

需要研究下Robot.java的disabledInit函数时候，能不能拿到电机的当前Position，如果可以，这个时候写个文件存盘，下次恢复时候可以用，不用手动复位了


状态机：
10. 初始状态，Elevator最低，手臂缩回 （zero状态）
20. Enable-> Elevator不动，手臂去到准备接珊瑚位置（base状态）
30. 如果检测到持有珊瑚，且ControlPad设定过目标（ControlPadHelper.ControlPadInfo有内容)，则等待按键触发抬手臂动作（根据ControlPadInfo的内容，Elevator和Arm同时进入L1,L2,L3,L4，4个状态之一）。此过程和重复，也就是说如果ControlPadInfo改变了，再次按键，会进入新的高度。
40. 无论何时检测到失去珊瑚（无论是主动发射的，还是其他原因）则回到20状态）



按键规划：
A（1） 自动瞄准，同时上层升起
B (2) Intake,启动，再按释放
X (3) null
Y (4) null
LeftBumper（5） zeroGyro
RightBumper(6)  长按，机器按照相对自身位置前后移动
SELECT(7) 机器上层归位，关机前记住要归位
OK(8) 上层CanCode归零，手掰过motor以后，要记得归零 (仅test模式有效)

2/14 TODO:
1. 验证光学传感器的延时功能有效 （有效）
2. 验证JoyStickButtonEx在Disable状态下有效(已验证，无效)

////////////////////////////////////////////
LED:
25lights 8-32
Status Lights: 
IDLE #0f4ff2(15, 79, 242)
WithCoral #f5f242(245, 242, 66)

Coral Lights:
L0 #ffffff(255, 255, 255)
L1 #eb1010(235, 16, 16)
L2 #0af712(10, 247, 18)
L3 #a807de(168, 7, 222)

Parts:
P1: 12, P2: 13
No Selection:
P1+P2: Status Lights
WithSelection:
If select Right side corals:
P1: Status Lights P2: Coral Lights
If select Left side corals:
P1: Coral Lights P2: Status Lights

Example:
Right L2:
Status x12 #0af712 x13

Left L3:
#a807de x12 Status x13

IDLE:
#0f4ff2 x25

////////////////////////////////////////////

2/15 TODO:
去掉一切试图在Disable状态下的外部输入操作，基本不可行 (待真机测试)
Elvator上下时候加一个障碍点，在通过障碍点的时候调整手臂姿态，避免碰到，同时也避免等Elevator到达目的地后再调整手臂姿态，导致的延时(done)
CANdle代码 (done)
Disable后，UpperSystem状态重置，避免下次Enable后莫名移动
ControlPad加入机器UpperSystem的状态同步(done)
ControlPad加入虚拟上下左右按钮，用于微调机器，用于人工瞄准
恢复底盘代码(done)
Test调试模式，支持Elevator解锁，支持重置UpperSystem状态
CurrentLimit

intake改双传感器 
s0 靠近入口
s1 靠近出口

先考虑进入状况：
option1： s0，s1 all on  慢速反转直到s1 off
option2： s0 on 减速，直到s1 on
出情况
s0，s1 all off


usb camera ip: 10.55.15.30